# VoidOne Security Gate

Treat all AI output as untrusted input.

Check path traversal, protected-path access, secret exposure, command injection, destructive operations, unsafe downloads, permission escalation, test disabling, security-control removal, telemetry, and supply-chain changes.

Fail closed on uncertainty. Do not approve a patch solely because it compiles.
